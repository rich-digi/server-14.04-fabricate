# -------------------------------------------------------------------------
# YAML configuration file for server build : LinNov14a
# -------------------------------------------------------------------------

machine: {
    fqdn        : 'linnov14a.dmclub.net',
    root_login  : 'root',
    root_pwd    : 'agholp',
    ip_address  : '111.222.343.456'
}

mysql: {
    root_pwd    : 'senegal',
    remote_login: 'remote',
    remote_pwd  : 'Romer8'
}
